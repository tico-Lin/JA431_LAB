# i18n 程式碼檢查報告

## 1. 發現硬編碼 (Hardcoded) 的檔案與位置

系統中確實存在部分尚未整合進 `i18n.ts` 的硬編碼中文字串，這些需要後續抽離至語系檔：

1. **`src/hooks/useSkillsData.ts` (第 14-80 行)**
   - 這裡直接使用了 `CATEGORY_NAME_ZH`, `CATEGORY_DESC_ZH`, `SKILL_NAME_ZH`, `SKILL_DESC_ZH`, `ROLE_ZH` 等常數物件來處理 `zh-TW` 的翻譯。這不符合 i18n 的最佳實踐，且若未來透過 PR Generator 新增技能或領域，這些硬編碼將無法動態翻譯新資料。
2. **`src/pages/PRGeneratorPage.tsx` (第 730-805 行)**
   - Tab 標籤：`label: '主持人設定'`、`t('admin.tabHomeConfig', '首頁設定')` (這類雖然包了 `t`，但在 `i18n.ts` 中並未設定 `admin` 相關的 key，因此實際上是依賴 fallback 的硬編碼)。
   - 頁面標題與描述：`系統資料管理 (Admin Panel)`、`集中管理全站靜態資料，直接同步至 GitHub`。
3. **`src/hooks/useDataSync.ts` (第 54, 60 行)**
   - 錯誤訊息如 `'無法儲存：您的 Token 密碼無效或已經過期...'` 以及 `'儲存失敗了。可能是網路問題...'` 皆為直接寫死的中文。

_(註：依您的指示，目前暫不對上述硬編碼直接進行修改。)_

---

## 2. 用詞專業度與白話文改善建議 (需與您討論)

我在檢閱 `i18n.ts` 及部分程式碼後，挑選出幾處稍嫌口語、白話，或可更具專業感的文案，請您過目並討論是否採用：

### 【能力落差分析 / Gaps】

- **目前**：`'好消息！所有核心能力都有覆蓋。'`
  **建議**：`'實驗室已具備所有核心技術量能。'`
  **原因**：「好消息！」過於口語，作為專業實驗室網站，直述具備量能即可。
- **目前**：`'快速看出哪些能力已具備、哪些能力適合外部委託或共同合作。'`
  **建議**：`'評估現有研究量能，辨識可由內部執行或需尋求外部產學合作與委託之技術項目。'`
  **原因**：「快速看出」較為白話，轉換為「評估...辨識...」更具學術與專業網站的語氣。

### 【委託檢測 / Vendor】

- **目前**：`'請選擇您需要的分析項目並填寫樣品資訊，系統將自動產生估價單並可直接寄送給負責窗口。'`
  **建議**：`'請選擇所需之分析項目並填寫樣品資訊，系統將自動生成估價單，並可供下載或寄送至專案窗口。'`
  **原因**：修飾語氣，使之更貼近一般企業或機構的標準服務流程說明。
- **目前**：`'請貼上您先前生成的估價單純文字內容，系統將嘗試自動填寫表單。'`
  **建議**：`'請貼上既有估價單之純文字內容，系統將自動帶入表單資訊。'`
  **原因**：「將嘗試自動填寫」聽起來系統不確定性較高，改為「自動帶入」是更標準的 UI 用語。
- **目前**：`'您在 {{date}} 有未完成的委託單，是否要恢復進度？'`
  **建議**：`'系統偵測到您於 {{date}} 有一筆未完成之委託單，是否接續填寫？'`
  **原因**：「恢復進度」稍微口語，改為「接續填寫」更平鋪直敘。

### 【實驗室主持人 / PI】

- **目前**：`heroTitle: '許峰豪助理教授 / PI'`
  **建議**：`'許峰豪 助理教授'` 或 `'許峰豪 助理教授 (PI)'`
  **原因**：「許峰豪助理教授 / PI」讀起來稍微冗長，且該頁面已名為「PI 介紹」，建議精簡。

### 【系統同步錯誤訊息 / DataSync】

- **目前**：`'儲存失敗了。可能是網路問題，或是檔案格式有誤。'`
  **建議**：`'儲存失敗：請確認網路連線狀態或檔案格式是否正確。'`
  **原因**：系統錯誤訊息加上「了」太像口語對話，「可能是...或是...」也不夠俐落。

---

以上是我盤點出的硬編碼位置以及文案修改建議。您可以針對第 2 部分告訴我哪些需要套用修改，或是您有其他更好的寫法，我們再來進行替換！
